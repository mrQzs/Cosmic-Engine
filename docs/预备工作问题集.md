# Cosmic-Engine 预备工作问题集

> 在生成数据库 Schema、GraphQL SDL、目录结构、依赖清单、Shader 原型、主题配置、Docker 环境、测试策略等预备文件之前，需要 Mr.Q 确认以下问题。
>
> **回答方式**：每题后面直接写答案即可，不确定的写"你决定"或"待定"。无需一次答完，可以分批回复。

---

## 一、项目全局决策（12 题）

### 1.1 博客定位
1. **博客主要语言**：文章以中文为主，还是中英双语？是否需要从第一版就支持 i18n 多语言切换？第一版就支持 i18n 多语言切换,省的后面要改
2. **作者模式**：单人博主（仅你一人写文章），还是未来可能邀请其他人投稿（多作者）？多作者把
3. **目标受众**：主要面向国内用户（影响 CDN 节点、搜索引擎优先级），还是面向全球？面向全球,可以加入CDN配置设置,但是默认不开启
4. **已有内容**：是否有需要迁移的旧博客文章？如果有，是什么格式（Markdown/HTML/WordPress 导出/其他）？大约多少篇？就不迁移了

### 1.2 规模预估
5. **文章量级**：预计上线第一年会发布多少篇文章？未来 3-5 年呢？（影响数据库索引策略和 3D 场景天体上限）每个月60-100篇的话
6. **评论量级**：预计单篇文章的平均评论数？热门文章的峰值评论数？（影响轨道环带算法的环数配置和 InstancedMesh 预分配）你决定,这个数据目前无法估算;
7. **并发访客**：预期峰值同时在线人数？（10 人级 / 百人级 / 千人级——影响 WebSocket 架构选择和 Redis 配置）你决定,不过按千人级也可以;

### 1.3 域名与部署
8. **域名**：是否已经有域名？如果有，是什么？（影响 Nginx 配置、SSL 证书、CDN 域名规划）wo.city,目前这个域名用在别的服务器上,可以转移

9. **服务器**：计划用什么服务器/云平台？（自有 VPS / 阿里云 / AWS / Cloudflare Pages + Workers / 其他）服务器配置大约是？（CPU 核数、内存、地区）自由VPS,

10. Basic System Information:
    ---------------------------------
    Uptime     : 1 days, 13 hours, 21 minutes
    Processor  : AMD EPYC 9655 96-Core Processor
    CPU cores  : 9 @ 2599.998 MHz
    AES-NI     : ✔ Enabled
    VM-x/AMD-V : ✔ Enabled
    RAM        : 15.6 GiB
    Swap       : 1024.0 MiB
    Disk       : 99.9 GiB
    Distro     : Debian GNU/Linux 12 (bookworm)
    Kernel     : 6.1.0-9-amd64
    VM Type    : KVM
    IPv4/IPv6  : ✔ Online / ✔ Online

11. **对象存储**：3D 贴图和图片存储选用哪家？（Cloudflare R2 / 阿里云 OSS / AWS S3 / 自建 MinIO / 其他）

    目前决定用本地服务器存储,但是这些R2 OSS S3 minio,可以提供设置页面进行配置;

12. **CDN**：是否使用 Cloudflare 代理主站（免费 DDoS 防护 + 缓存），还是仅用 CDN 加速静态资源？

    后续会使用,目前不使用先

13. **预算约束**：每月在基础设施上的预算范围？（影响选型：Meilisearch 需要额外内存、Ollama 需要 GPU 等）

    你看下配置是否符合,GPU的话,能否改成用户这边的来帮忙,服务器没有GPU

---

## 二、后端技术选型（10 题）

### 2.1 Go 后端
13. **Go 版本**：使用 Go 1.22+ 还是最新稳定版（当前 1.24）？最新稳定版
14. **Web 框架**：**Fiber** 还是 **Gin**？（Fiber 性能更高但生态略小，Gin 更主流稳定）Fiber,可以自己实现功能
15. **ORM 选型**：**Ent**（图式 ORM，声明式定义 Schema，自动迁移）还是 **sqlc**（SQL 优先，从 SQL 生成 Go 代码，更贴近原生）？你决定
16. **全文搜索**：优先用 **PostgreSQL 内置**（tsvector + zhparser/pg_jieba），还是独立部署 **Meilisearch**？（Meilisearch 搜索体验更好但多一个服务要维护）优先内置
17. **Embedding 模型**：文章向量嵌入用什么方案？D,到时候我会整合一个文章发布系统,里面做了向量嵌入,可以直接使用;
    - A) 本地 Ollama 运行 bge-m3（需要 ~4GB 内存）
    - B) 调用外部 API（如 Claude API / OpenAI Embeddings，按量计费）
    - C) 第一版先不做语义搜索，用 Jaccard 标签相似度替代

### 2.2 数据库
18. **PostgreSQL 版本**：15 还是 16+？（16 支持更多并行查询优化）16
19. **Redis 版本**：用 Redis 7 社区版还是 **Redis Stack**？（Stack 内置 Bloom Filter、JSON 模块，但镜像更大）Redis Stack
20. **数据库管理工具**：是否需要在 Docker 环境中包含数据库管理面板（如 pgAdmin / Adminer）？不需要
21. **备份策略**：自动备份到 Git 仓库（PLAN 13.14）从第一版就做，还是后续再加？后续再加
22. **评论审核**：新评论默认直接发布（无审核），还是默认需要审核后才显示？无审核,但是去掉垃圾评论

---

## 三、前端技术选型（10 题）

### 3.1 框架与工具
23. **Next.js 版本**：使用 14 还是 15（当前最新）？是否使用 Turbopack（实验性但更快）？15,可以使用Turbopack
24. **包管理器**：**pnpm**（推荐，磁盘效率高）还是 **yarn** 还是 **npm**？pnpm
25. **CSS 方案**：纯 **Tailwind CSS** 还是 Tailwind + **CSS Modules**（部分组件用 Modules 隔离）？你决定,那种性能好,效果好就行
26. **GraphQL 客户端**：**urql**（轻量 ~7KB）还是 **Apollo Client**（功能全但 ~30KB）？ **Apollo Client**
27. **Markdown 编辑器（管理后台）**：**Monaco Editor**（VSCode 内核，重但功能强）还是 **Milkdown**（轻量所见即所得）还是 **简单 textarea + 实时预览**？Monaco Editor

### 3.2 3D 场景
28. **初始摄像机视角**：首次打开页面时，摄像机从什么角度看宇宙？选A
    - A) 远景全貌——看到整个宇宙的所有星系（像从太空站俯瞰）
    - B) 中景——位于某个星系边缘，能看到该星系和远处的其他星系
    - C) 近景——直接停在最新文章的行星旁边
29. **星系数量**：计划创建几个星系（分类）？每个星系大致主题是什么？（如："后端"、"前端"、"随笔"、"安全"……这影响 3D 空间布局参数）七大类,杂志,日常,新闻,文章,书籍,论文,小说; 但是每个大类还有一些子类...杂志分成了科技,医学,文学,科学,经济等等
30. **背景音乐/音效**：选A
    - A) 第一版就集成音效系统（深空氛围音 + 交互音效）
    - B) 第一版静音，后续版本再加
    - C) 不要音效

---

## 四、UI / 视觉设计（18 题）

> 这部分问题直接决定 Tailwind 主题配置、Shader 参数、HUD 组件样式。

### 4.1 整体色调
31. **主色调偏好**：深空背景之上，UI 元素（按钮、面板边框、高亮）的主色调偏好？A+C
    - A) 冷蓝/青色系（科幻飞船感）
    - B) 暖琥珀/金色系（复古太空歌剧感）
    - C) 紫色/品红色系（赛博朋克感）
    - D) 绿色系（黑客终端感）
    - E) 混合搭配（比如主色蓝 + 强调色橙）
    - F) 其他偏好（具体描述）
32. **深空背景色**：纯黑 `#000000` 还是深蓝黑 `#0a0a1a` 还是深紫黑 `#0a0614`？真实太空颜色的
33. **UI 面板透明度**：HUD 面板的毛玻璃透明度偏好？B
    - A) 高透明（能清晰看到背后的星空，信息覆盖感弱）
    - B) 半透明（背景模糊但可辨认，信息与场景平衡）
    - C) 低透明（接近不透明，信息优先，背景仅边缘可见）

### 4.2 字体
34. **正文字体**：PLAN 中建议 `Inter`（无衬线西文）。中文正文字体偏好？C
    - A) 系统默认（`-apple-system, "PingFang SC", "Microsoft YaHei"`）
    - B) 思源黑体（Noto Sans SC，需要加载 ~2MB 字体文件）
    - C) 霞鹜文楷（LXGW WenKai，带书法韵味，适合人文内容）
    - D) 其他
35. **代码/HUD 字体**：`JetBrains Mono` 确认？还是偏好 `Fira Code`（带连字）或 `Source Code Pro`？你决定

### 4.3 HUD 风格
36. **HUD 整体风格**：不同主题使用不同HUD
    - A) 军事/战术风（粗边框、直角、数据密集、雷达扫描线风格）
    - B) 极简科幻风（细线边框、大留白、优雅渐变）
    - C) 复古 CRT 风（扫描线、荧光色、屏幕弯曲、像素颗粒感）
    - D) 生物科技风（有机曲线边框、脉动动画、半透明气泡）
37. **HUD 边框样式**：面板边框使用实线还是虚线/点线？边框是否带发光效果（glow/neon）？点线加发光
38. **HUD 动画**：面板弹出/收起时的动画偏好？D
    - A) 从边缘滑入（slide）
    - B) 从中心缩放弹出（scale）
    - C) 逐行扫描式展开（像终端输出一样从上到下逐行出现）
    - D) 粒子凝聚（粒子从四周聚合成面板形状）

### 4.4 文章阅读面板
39. **阅读面板位置**：大气突入后，文章阅读面板在屏幕上的位置？A
    - A) 居中（占屏幕 70% 宽度，两侧可见行星表面）
    - B) 右侧（左侧保留行星的 3D 视图）
    - C) 全屏覆盖（行星表面作为背景全屏虚化）
40. **阅读面板最大宽度**：文章正文区域的最大宽度？（如 `700px` / `800px` / `900px`，影响长文阅读体验）占浏览器的百分之70就好
41. **代码块主题**：代码高亮配色偏好？A
    - A) One Dark Pro（深色暖色调，最流行）
    - B) Dracula（深色紫色调）
    - C) GitHub Dark（深色中性灰）
    - D) Monokai（深色高对比）
    - E) 自定义配色（配合宇宙主题的蓝/紫调）

### 4.5 CRT 终端
42. **CRT 文字颜色**：黑洞解密的 CRT 终端文字颜色？C
    - A) 经典绿（`#00ff00`，黑客帝国感）
    - B) 琥珀色（`#ffb000`，复古终端感）
    - C) 冰蓝色（`#00d4ff`，科幻感）
43. **CRT 扫描线强度**：扫描线效果的可见度？B
    - A) 强（明显的水平线条，复古感拉满）
    - B) 轻微（隐约可见，不影响阅读）
    - C) 无（不要扫描线效果）

### 4.6 加载页面
44. **加载页面风格**：宇宙初始化时的加载页面设计方向？B
    - A) 文字终端风（黑屏 + 逐行打字输出系统启动信息，如 "Initializing cosmic engine...", "Compiling shaders 3/7..."）
    - B) 粒子凝聚风（散乱的星尘粒子逐渐聚合成一个球体/logo，进度条环绕）
    - C) 极简进度条（中央一个细长的发光进度条 + 百分比数字）
    - D) 其他
45. **自定义 Logo/标志**：是否有博客 Logo 或文字标志？如果有请提供；如果没有，是否需要在加载页面显示博客名称（什么名称）？目前还没设计logo,可以保留位置;网站名是CyberGeek 副标题是CyberCode Chronicles;

### 4.7 移动端
46. **移动端 2D 降级的视觉方向**：低端设备的 2D 降级视图长什么样？B
    - A) 传统博客卡片列表（但用宇宙主题配色 + 星空背景 CSS 动画）
    - B) 简化的 2D 星图（CSS 绘制的静态星点 + 可点击的圆形天体节点）
    - C) 瀑布流时间线（每篇文章是一个时间节点，深色背景带粒子动画）
47. **移动端导航**：移动端如何在星系/分类间切换？B
    - A) 底部 Tab 栏（每个 Tab = 一个星系）
    - B) 侧边抽屉菜单（从左侧滑出星系列表）
    - C) 顶部下拉选择器

### 4.8 其他视觉细节
48. **行星自转速度**：偏好快速自转（活泼、吸引注意力）还是缓慢自转（沉稳、沉浸感）？大约几秒转一圈？（如 10 秒 / 30 秒 / 60 秒）选个默认值,后续可调

---

## 五、交互设计（12 题）

### 5.1 导航与操控
49. **摄像机控制方式**：用户在宇宙中自由探索时的操控方式？选D
    - A) 鼠标拖拽旋转 + 滚轮缩放（类似 Google Earth）
    - B) WASD 键盘飞行 + 鼠标转向（类似 FPS 游戏）
    - C) 仅点击天体跳转，不支持自由漫游（引导式体验）
    - D) A + C 混合（支持自由拖拽，也支持点击跳转）
50. **飞行速度**：摄像机从一个星系飞到另一个星系大约多长时间？选B
    - A) 1-2 秒（快速切换，效率优先）
    - B) 3-5 秒（中等，有旅途感但不拖沓）
    - C) 5-8 秒（缓慢，享受飞行过程）
51. **ESC 键行为**：按 ESC 时的响应？A
    - A) 逐层返回（阅读面板 → 行星视角 → 星系视角 → 宇宙全景）
    - B) 直接返回宇宙全景
    - C) 返回上一个位置（浏览器历史风格）

### 5.2 评论交互
52. **评论者身份**：评论者需要提供什么信息？选c
    - A) 仅昵称（最低门槛）
    - B) 昵称 + 邮箱（邮箱用于通知回复，不公开展示）
    - C) 昵称 + 邮箱 + 个人网站 URL（可选）
    - D) 需要 GitHub/Google 第三方登录
53. **评论内容格式**：评论支持 Markdown 还是纯文本？选C
    - A) 纯文本（简单，不用担心 XSS）
    - B) 支持基础 Markdown（加粗、链接、行内代码，不支持图片/HTML）
    - C) 完整 Markdown（含代码块高亮）

### 5.3 管理后台
54. **管理后台入口**：管理后台在 `/admin` 路由下（同一个 Next.js 应用内），还是完全独立的前端应用（独立域名 `admin.xxx.com`）？/admin_199209173332这样访问
55. **管理后台认证方式**：仅密码登录，还是支持 TOTP 两步验证（Google Authenticator）？TOTP
56. **文章编辑方式**：在管理后台写文章，还是在本地用 VS Code / Obsidian 写好 Markdown 后通过 API 或 Git 推送发布？本地写好,通过API

### 5.4 阅读体验
57. **文章阅读时 3D 场景状态**：阅读文章时，背景的 3D 场景如何处理？选C
    - A) 持续渲染（行星在背后缓慢自转，有沉浸感但耗电）
    - B) 冻结在当前帧（截图为静态背景，省电）
    - C) 降级渲染（帧率降至 15fps，节省资源但保留动态感）
58. **暗色/亮色模式**：宇宙主题天然是暗色的。是否需要提供亮色阅读模式？选A
    - A) 仅暗色（保持沉浸感统一）
    - B) 阅读面板内可切换亮色背景（仅文章正文区域变白）
    - C) 全站暗/亮切换
59. **文章目录 (TOC)**：长文章的目录位置？选B
    - A) 左侧固定（始终可见，占用空间）
    - B) 右侧浮动（悬停展开）
    - C) 顶部折叠（点击展开目录抽屉）
    - D) 不需要目录
60. **阅读进度是否记忆**：用户关闭文章后再次打开，是否自动滚动到上次阅读位置？（localStorage 存储）可以

---

## 六、功能优先级与裁剪（8 题）

> 以下功能在 PLAN 中都有设计，但第一版（MVP）不一定全做。明确优先级可以避免过度工程。

61. **MVP 必须包含的天体类型**（勾选）：
    - [x] 星系 Galaxy（分类）
    - [x] 行星 Planet（文章）
    - [x] 卫星 Satellite（评论）
    - [ ] 小行星带 Asteroid（草稿/灵感）
    - [ ] 虫洞 Wormhole（时间轴归档）
    - [x] 脉冲星 Pulsar（个人简介）
    - [x] 黑洞 Black Hole（加密文章）
    - [ ] 友链星际门 StarGate

62. **MVP 必须包含的后端功能**（勾选）：
    - [x] GraphQL API
    - [x] RESTful API
    - [x] RSS Feed
    - [ ] Webhook 推送
    - [ ] 邮件订阅 Newsletter
    - [x] 全文搜索
    - [ ] 文章版本历史
    - [x] 内容导出（PDF/EPUB）
    - [x] 文件/图片上传
    - [x] 访客统计
    - [ ] 评论通知（邮件）

63. **MVP 必须包含的前端功能**（勾选）：
    - [x] 大气突入动画
    - [x] 卫星发射动画
    - [x] 虫洞跃迁动画
    - [x] 小地图
    - [x] 搜索 3D 可视化
    - [x] 星座关联连线
    - [x] 音效系统
    - [ ] PWA 离线支持
    - [x] i18n 多语言
    - [ ] 社交分享
    - [ ] 代码沙盒（交互式运行）
    - [ ] 在线用户飞船可视化

64. **后处理特效优先级**：以下特效哪些是第一版必须有的？
    - [x] Bloom 泛光
    - [x] Chromatic Aberration 色散
    - [x] GlitchPass 画面撕裂
    - [x] God Rays 体积光
    - [x] SSAO 环境光遮蔽
    - [x] Depth of Field 景深
    - [x] SMAA 抗锯齿

65. **管理后台 MVP 范围**：第一版管理后台需要哪些功能？
    - [x] 文章 CRUD（含 Markdown 编辑器）
    - [x] 评论管理（审核/删除）
    - [x] 星系管理（创建/编辑分类）
    - [x] 草稿管理
    - [ ] 加密文章创建
    - [x] 脉冲星简介编辑
    - [x] 友链管理
    - [x] Webhook 配置
    - [x] 订阅者管理
    - [x] 数据统计仪表盘
    - [x] 系统设置
    - [x] 媒体库（图片管理）

66. **WebSocket 实时功能是否进 MVP**：多人在线看到卫星发射等实时功能，是第一版就做还是后续迭代？
67. **自适应画质系统是否进 MVP**：4 级画质自动调节，还是第一版只做"高画质 + 2D 降级"两档？
68. **先进算法是否进 MVP**：以下哪些在第一版就用？
    - [x] 开普勒椭圆轨道（vs 先用正圆凑合）
    - [x] 注意力加权 LOD（vs 纯距离 LOD）
    - [x] 内容驱动天体美学（vs 纯随机 seed）
    - [x] pgvector 语义搜索（vs 纯标签搜索）
    - [x] 渐进式场景揭示（vs 简单 loading 一次性加载）
    - [x] 摄像机意图预测（vs 用户点击后再加载）
    - [x] Curl Noise 星云（vs 简单随机粒子）
    - [x] Reaction-Diffusion 纹理（vs Voronoi 即可）

---

## 七、内容与运营（6 题）

69. **初始星系（分类）规划**：第一版计划创建哪几个星系？请列出名称和大致主题。例如：杂志-科技/科学/经济;文章-精选;论文-摘选;小说;
    
    ```
    示例:
    - "锻造炉" (Forge) → 后端/系统编程 (Go, Rust, Linux)
    - "量子域" (Quantum) → 前端/可视化 (React, Three.js, CSS)
    - "星际图书馆" (Library) → 读书笔记/知识管理
    - "暗物质" (Dark Matter) → 安全/密码学
    - "星云" (Nebula) → 随笔/生活/哲学
    ```
70. **个人简介内容**：脉冲星简介页面需要展示什么？目前还没想好,可以弄个模板;
    
    - 技术栈雷达图中包含哪些技能？（列出技能名 + 熟练度 0-100）
    - 经历时间线有几个关键节点？
    - 社交链接有哪些？（GitHub / Twitter / 邮箱 / 微信 / 其他）
71. **版权声明**：文章默认使用什么版权协议？（CC BY-NC-SA 4.0 / CC BY 4.0 / 保留所有权利 / 其他）开源的,不可商用的许可
72. **评论引导语**：评论输入框的默认 placeholder 文案？（如 "留下你的星际通讯..."、"发射你的想法..." 或其他）"留下你的星际通讯..."、"发射你的想法..." ,等等这些跟宇宙相关的文案,每篇文章的评论的placeholder 尽量不一样
73. **404 页面**：访问不存在的页面时显示什么？A,b,C都有,每次不一样
    
    - A) 迷失在太空中的漂浮宇航员
    - B) 坐标不存在的虚空区域（黑暗 + "未知坐标"文字）
    - C) 被黑洞吞噬的画面
74. **站点名称与 Slogan**：博客的正式名称和副标题/Slogan？（显示在浏览器标题栏、SEO、RSS 中）标题CyberGeek,副标题 CyberCode Chronicles

---

## 八、开发流程与协作（6 题）

75. **Git 仓库**：是否已创建 GitHub/GitLab 仓库？仓库名称？公开还是私有？还没有,许可证不知道选啥
76. **CI/CD 平台**：使用 GitHub Actions 还是其他 CI？（GitLab CI / Jenkins / 其他）使用GitHub Actions
77. **开发机器**：你的开发机操作系统？（Windows 11，已知）CPU / GPU / 内存配置？（影响 Docker 资源分配和 3D 开发调试）我开发机器是debian 12
78. **Node.js 版本**：当前安装的 Node.js 版本？或者偏好使用哪个 LTS 版本？还没开始安装
79. **代码编辑器**：使用 VS Code？是否安装了 GLSL 语法高亮和 Three.js 类型提示插件？都还没有
80. **阶段交付节奏**：每个 Phase 大概预期的开发时间？是全职投入还是业余时间开发？（影响我建议的任务拆分粒度）全职投入

---

## 九、安全与合规（4 题）

81. **HTTPS**：是否使用 Let's Encrypt 免费证书？还是已有付费证书？需要通配符证书（`*.domain.com`）吗？已经有免费证书
82. **隐私合规**：是否需要遵守 GDPR（面向欧洲用户）？是否需要 Cookie 同意弹窗？没啥隐私,你来考虑
83. **评论 IP 存储**：评论者的 IP 地址存储多久？（仅存哈希 / 存原始 IP 但 30 天后删除 / 不存储）不存ip是不是更好?
84. **加密文章数量预估**：预计会有多少篇加密文章？（1-5 篇 / 5-20 篇 / 20+ 篇——影响黑洞区域的空间布局策略）其实没有加密文章; 

---

## 十、开放问题（Mr.Q 主动补充）

85. **你对这个项目最在意的一个方面是什么？**（性能 / 视觉效果 / 内容管理便捷性 / 安全性 / 代码质量 / 其他）视觉效果加性能
86. **你最担心的技术风险是什么？**性能不是很好
87. **有没有其他博客/网站的某个具体效果特别想模仿或超越？**（提供 URL 或描述）https://paveldogreat.github.io/WebGL-Fluid-Simulation/ 流体力学的实现,非常流畅的处理
88. **以上问题中是否有你觉得不重要、不需要回答的？或者有我遗漏的你想特别说明的点？**目前没有

---

> **回答完毕后**，我将根据你的回答并行生成以下全部预备文件：
>
> 1. `database/001_init_schema.sql` — 完整可执行的数据库迁移
> 2. `graph/schema.graphqls` — 完整 GraphQL Schema SDL
> 3. `PROJECT_STRUCTURE.md` — 前后端完整目录结构与文件清单
> 4. `DEPENDENCIES.md` — 前后端依赖清单（含版本和用途）
> 5. `shaders/` — 6 个关键 GLSL Shader 原型
> 6. `tailwind.config.ts` + `styles/cosmic-theme.css` — 宇宙主题配置
> 7. `docker-compose.dev.yml` — 开发环境 Docker 编排
> 8. `.env.example` — 环境变量模板
> 9. `TESTING_STRATEGY.md` — 测试策略文档
> 10. `shared/types/ws.ts` — WebSocket 消息协议完整定义
> 11. `SECURITY_CHECKLIST.md` — 安全检查清单
> 12. `ADR/` — 关键技术决策记录
